version: 1
frontend:
    phases:
        build:
            commands:
                - "pip3 install --no-cache-dir -r requirements.txt --verbose"
    artifacts:
        baseDirectory: /
        files:
            - '**/*'
    cache:
        paths: []